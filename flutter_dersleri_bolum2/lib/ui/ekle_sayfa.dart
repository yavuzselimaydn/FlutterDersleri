import 'package:flutter/material.dart';

class Eklesayfa extends StatelessWidget {
  const Eklesayfa({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.lightBlue.shade500,
      child: const Center(
        child: Text("EKLEME SAYFA"),
      ),
    );
  }
}
