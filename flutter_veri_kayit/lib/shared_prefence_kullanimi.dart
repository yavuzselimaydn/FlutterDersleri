import 'package:flutter/material.dart';

class SharedPrefenceKullanimi extends StatelessWidget {
  const SharedPrefenceKullanimi({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("shared prefence kullanimi"),),
      body: ListView(),
    );
  }
}